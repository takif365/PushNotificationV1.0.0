name = "push-scheduler"
main = "worker.js"
compatibility_date = "2024-01-01"

# Trigger every minute
[triggers]
crons = ["* * * * *"]

# Define environment variables placeholder (User must set these in Cloudflare Dashboard)
[vars]
# CRON_SECRET = "your-secret-here"
